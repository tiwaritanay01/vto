{
  "name": "vto-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "cd vto-frontend && npm install && npm run build",
    "start": "uvicorn api.vto.main:app --host 0.0.0.0 --port ${PORT:-8080}"
  },
  "dependencies": {}
}
